<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
    <strong>Image:</strong>
    <% @post.images.each do |uploaded| %>
      <%= image_tag uploaded.image.url %>
    <% end %>
  </p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
